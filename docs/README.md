# 📅 JXPU 课表订阅服务 (Hono 重构版)

基于 Node.js (Hono + Native Fetch) 构建的江西职业技术大学课表自动订阅系统。支持教务系统二维码扫码登录，生成 ICS 订阅链接，可无缝同步至 iOS、Google Calendar、Outlook 等日历应用。

## ✨ 核心特性

- **🚀 高性能架构**: 从 Express 迁移至 [Hono](https://hono.dev/)，配合 Node.js 18+ 原生 Fetch API，显著降低内存占用并提升响应速度。
- **🔐 安全扫码登录**: 对接学校 CAS 统一身份认证系统，支持二维码扫码，无需存储用户明文账号密码。
- **🔄 智能会话管理**:
  - 内存级 Session 隔离，防止并发扫码冲突。
  - 自动处理 Cookie 传递链，支持 SESSION 与 TGC 的自动获取与续期。
  - 自动重试机制：SESSION 获取失败时自动重试（最多3次）。
- **📅 标准 ICS 输出**: 生成符合 RFC 5545 标准的日历文件，包含课程名称、地点、教师及**课前35分钟提醒**。
- **📱 全平台适配**: 提供完整的前端交互界面，支持手机端直接操作。

## 🛠️ 技术栈

- **Runtime**: Node.js >= 18.0.0 (必需)
- **Web Framework**: Hono + @hono/node-server
- **HTTP Client**: Native Fetch API (配合 HTTPS Agent)
- **Parser**: Cheerio (HTML 解析)
- **Calendar**: Ical-generator
- **Database**: SQLite3 (轻量级持久化存储)

## 📦 快速部署

### 1. 环境准备
   确保安装 Node.js 18 或更高版本。

### 2. 安装依赖
   ```bash
   npm install
   ```

### 3. 配置 (可选) 修改 src/auth.js 中的 COUNT_ID 可添加百度统计 ID。

### 4. 启动服务
  ## 开发模式 (热重载)
  ```bash
    npm run dev
  ```
  ## 生产模式
  ```bash  
    npm start
  ```
### 5. 访问 打开浏览器访问 `http://localhost:3000`

